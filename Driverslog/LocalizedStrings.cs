namespace Driverslog {
    public class LocalizedStrings {
        
        private static readonly Strings Strings = new Strings();

        public Strings Localized { get { return Strings; }}

    }
}