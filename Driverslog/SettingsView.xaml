<phone:PhoneApplicationPage
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro" 
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls" 
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit" 
    xmlns:Converters="clr-namespace:Driverslog.Converters" x:Class="Driverslog.SettingsView"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="False">

    <phone:PhoneApplicationPage.Resources>
        <Converters:YesNoToIndexConverter x:Key="YesNoToIndexConverter" />
    </phone:PhoneApplicationPage.Resources>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot">
        <controls:Pivot Title="{Binding Path=Localized.AppTitle, Source={StaticResource Strings}}">
            <controls:PivotItem Header="{Binding Path=Localized.SettingsViewTitle, Source={StaticResource Strings}}">
                <ScrollViewer>
                    <StackPanel>
                        <TextBlock Text="{Binding Path=Localized.SettingsDefaultCar, Source={StaticResource Strings}}" Style="{StaticResource PhoneTextNormalStyle}"/>
                        <TextBox x:Name="Car"/>

                        <TextBlock Text="{Binding Path=Localized.SettingsDefaultExportToEmail, Source={StaticResource Strings}}" Style="{StaticResource PhoneTextNormalStyle}"/>
                        <TextBox x:Name="Email" InputScope="EmailNameOrAddress"/>

                        <TextBlock Text="{Binding Path=Localized.SettingsDistanceUnit, Source={StaticResource Strings}}" Style="{StaticResource PhoneTextNormalStyle}"/>
                        <TextBox x:Name="DistanceUnit" />

                        <TextBlock Text="{Binding Path=Localized.SettingsHideOdometerFields, Source={StaticResource Strings}}" Style="{StaticResource PhoneTextNormalStyle}"/>
                        <toolkit:ListPicker 
                            SelectedIndex="{Binding HideOdoFields, Converter={StaticResource YesNoToIndexConverter}}" 
                            cal:Message.Attach="[Event SelectionChanged] = [Action OnHideOdoFieldsChanged($eventArgs)]">
                            <ListBoxItem Content="{Binding Path=Localized.No, Source={StaticResource Strings}}"/>
                            <ListBoxItem Content="{Binding Path=Localized.Yes, Source={StaticResource Strings}}"/>
                        </toolkit:ListPicker>

                        <TextBlock Text="{Binding Path=Localized.SettingsHideMileageField, Source={StaticResource Strings}}" Style="{StaticResource PhoneTextNormalStyle}"/>
                        <toolkit:ListPicker 
                            SelectedIndex="{Binding HideMileageField, Converter={StaticResource YesNoToIndexConverter}}"
                            cal:Message.Attach="[Event SelectionChanged] = [Action OnHideMileageFieldChanged($eventArgs)]">
                            <ListBoxItem Content="{Binding Path=Localized.No, Source={StaticResource Strings}}"/>
                            <ListBoxItem Content="{Binding Path=Localized.Yes, Source={StaticResource Strings}}"/>
                        </toolkit:ListPicker>

                    </StackPanel>
                </ScrollViewer>
            </controls:PivotItem>
            <controls:PivotItem Header="{Binding Path=Localized.AboutViewTitle, Source={StaticResource Strings}}">
                <StackPanel>
                    <TextBlock Text="{Binding Path=Localized.AppTitle, Source={StaticResource Strings}}" Style="{StaticResource PhoneTextLargeStyle}"/>
                    <TextBlock x:Name="ApplicationVersion" Style="{StaticResource PhoneTextNormalStyle}"/>
                    <TextBlock Text="Copyright 2011 - 2012 Christofer Löf" Style="{StaticResource PhoneTextNormalStyle}"/>
                    <HyperlinkButton Content="{Binding Path=Localized.AboutTechSupport, Source={StaticResource Strings}}" NavigateUri="http://twitter.com/chrislof" TargetName="_blank" HorizontalContentAlignment="Left" />
                    <Button Content="{Binding Path=Localized.AboutRateAndReview, Source={StaticResource Strings}}" Height="72" Name="ReviewButton" HorizontalContentAlignment="Center" HorizontalAlignment="Center" Margin="0,24,0,0" Click="button1_Click" />
                </StackPanel>
            </controls:PivotItem>
        </controls:Pivot>
    </Grid>

</phone:PhoneApplicationPage>
